<%= form_for(current_user.votes.find_by(weekend_id: weekend.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.hidden_field :weekend_id %>
  <%= f.submit "unvote", class: "button button-destroy" %>
<% end %>