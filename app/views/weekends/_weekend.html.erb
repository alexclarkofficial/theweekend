  <div class="list-group-item">
      <div class="week"><h2><%= weekend.week.date %></h2></div>
      <div>
        <%= (weekend.votes.count) %> votes.
      </div>
      <%= render 'weekends/vote_form', :weekend => weekend %>
    <%= image_tag weekend.images.first.image.url(:thumb).to_s %>
  </div>