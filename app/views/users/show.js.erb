$(".weekends").append("<%= escape_javascript(render @weekends) %>");
$("a.load-more-weekends").attr('href', '<%= root_path page: @weekends.next_page %>');

<% unless @weekends.next_page %>
  $("a.load-more-weekends").remove()
<% end %>
