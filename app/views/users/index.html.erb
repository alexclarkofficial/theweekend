<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
   <%= render partial: 'user', collection: @users %>
   <%#= render partial: 'follow_form', collection: @users %>
</ul>

<%= will_paginate %>